# 2026-07-13

## UI 重构：需求列表移到侧边栏
- 新增 `src/sidebarProvider.ts`，实现 `WebviewViewProvider` 填充 VSCode 侧边栏
- 侧边栏显示需求列表、新建需求按钮，点击需求在主面板打开
- 简化 `src/extension.ts`：主面板只展示选中需求的流程阶段、产出文档、审核与执行操作
- 修复侧边栏空白和无限进度条问题
- 新增 MIT LICENSE，补充 package.json 的 `repository` 字段
- 编译通过、测试通过、打包成功，并推送到 GitHub
