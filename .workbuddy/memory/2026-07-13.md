# 2026-07-13

## 新建需求改为 /openflow:design
- `flowmaster.newDemand` 命令和 propose/design 阶段执行现在运行 `claude /openflow:design`
- 新增 `hasOpenflowDesignSkill()` 和 `ensureOpenflowDesignSkill()` 检查 `.claude/skills/openflow-design/SKILL.md` 或 `~/.claude/skills/openflow-design/SKILL.md`
- 若未检测到该技能，弹窗提示用户先添加 openflow-design 技能
- 编译、测试、打包均通过，并已推送到 GitHub

## 修复侧边栏空白/进度条问题
- 关闭扩展激活时自动打开主面板，避免激活阶段可能阻塞侧边栏注册
- 侧边栏 Webview 增加 8 秒超时提示：未收到响应则提示重新加载窗口
- 为 `flowmaster.newDemand` 命令补注册 package.json，使命令面板可见
- GitHub push 因网络不可达失败，本地仓库已提交最新代码

## 自动版本号递增规则
- 新增 `scripts/bump-version.js`：按 `major.minor.patch` 解析，patch +1，满 10 进位到 minor，minor 满 10 进位到 major
- 在 `package.json` 添加 `precompile` 脚本，每次 `npm run compile` 自动先执行版本递增
- 同时更新 `package-lock.json` 顶层版本号，保持锁定文件一致
- 已推送到 GitHub，当前版本 0.1.1

## 侧边栏刷新按钮 + 需求加载修复 + 内置终端新建需求
- 在 `src/sidebarProvider.ts` 标题栏添加刷新按钮，点击后发送 `refresh` 命令并显示旋转动画，收到 `stateUpdated` 后停止
- 修复 `src/stateReader.ts`：构造函数支持传入 `workspaceRoot`，避免使用扩展路径导致找不到 `.workflow/state`
- 修复 `src/extension.ts`：projectRoot 默认使用当前工作区根目录，并把 projectRoot 传给 `StateReader`
- 将 `runOpenflowDesign()` 和 design/propose 阶段的 `runPhase()` 从 VS Code 终端改为 `TerminalBridge.startProcess`，输出到插件内置下半部分终端
- 新增 `ensurePanelOpen()`，新建需求/执行 design 阶段时自动打开主面板
- 编译通过、49 个测试全过、打包成功，已推送到 GitHub，当前版本 0.1.4

## 修复侧边栏完全空白、主面板不显示
- `src/sidebarProvider.ts`：
  - 前端增加 `ready` 握手，扩展收到 `ready` 后再下发需求数据，避免 early postMessage 丢失
  - 增加 `fallback` 静态提示和 `window.onerror` 错误显示，即使 JS 失败也能看到状态
  - 为 `resolveWebviewView` 和消息处理增加 `try-catch`，错误直接显示在侧边栏
- `src/extension.ts`：扩展 `activate` 增加 `try-catch`，防止激活失败静默无响应
- 编译、测试、打包通过，版本升到 0.1.7
- GitHub push 因网络中断失败，本地已提交，稍后重试
