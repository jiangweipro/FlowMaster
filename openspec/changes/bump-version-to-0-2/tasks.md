## 1. Update Version

- [ ] 1.1 Update `version` field in `package.json` from `0.1.0` to `0.2.0`
- [ ] 1.2 Update `version` field in `package-lock.json` from `0.1.0` to `0.2.0`

## 2. Verify

- [ ] 2.1 Run `npm install` to ensure lockfile is consistent
- [ ] 2.2 Run `npm test` to confirm no regressions