# 2026-07-13

## 新建需求改为 /openflow:design
- `flowmaster.newDemand` 命令和 propose/design 阶段执行现在运行 `claude /openflow:design`
- 新增 `hasOpenflowDesignSkill()` 和 `ensureOpenflowDesignSkill()` 检查 `.claude/skills/openflow-design/SKILL.md` 或 `~/.claude/skills/openflow-design/SKILL.md`
- 若未检测到该技能，弹窗提示用户先添加 openflow-design 技能
- 编译、测试、打包均通过，并已推送到 GitHub

## 修复侧边栏空白/进度条问题
- 关闭扩展激活时自动打开主面板，避免激活阶段可能阻塞侧边栏注册
- 侧边栏 Webview 增加 8 秒超时提示：未收到响应则提示重新加载窗口
- 为 `flowmaster.newDemand` 命令补注册 package.json，使命令面板可见
- GitHub push 因网络不可达失败，本地仓库已提交最新代码

## 自动版本号递增规则
- 新增 `scripts/bump-version.js`：按 `major.minor.patch` 解析，patch +1，满 10 进位到 minor，minor 满 10 进位到 major
- 在 `package.json` 添加 `precompile` 脚本，每次 `npm run compile` 自动先执行版本递增
- 同时更新 `package-lock.json` 顶层版本号，保持锁定文件一致
