## Why

FlowMaster Dashboard has been stable at version 0.1.0 with several improvements and bug fixes. Bumping to 0.2.0 marks the next milestone, reflecting the accumulated changes since the initial release.

## What Changes

- Update `package.json` version field from `0.1.0` to `0.2.0`
- Update `package-lock.json` version field from `0.1.0` to `0.2.0`

## Capabilities

### New Capabilities

- *(none)* — no new capabilities are introduced by this change.

### Modified Capabilities

- *(none)* — no spec-level behavior changes.

## Impact

- `package.json` — single version field update
- `package-lock.json` — single version field update (auto-synced via `npm install` or manual edit)
- No code changes, API changes, or dependency updates