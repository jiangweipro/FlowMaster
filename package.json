{
  "name": "flowmaster-dashboard",
  "displayName": "FlowMaster Dashboard",
  "description": "VSCode Extension WebView UI for OpenFlow workflow management",
  "version": "0.1.8",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jiangweipro/FlowMaster"
  },
  "publisher": "flowmaster",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "icon": "media/icon-128.png",
  "activationEvents": [
    "onCommand:flowmaster.openDashboard",
    "onView:flowmaster-sidebar"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "flowmaster.openDashboard",
        "title": "FlowMaster: Open Dashboard"
      },
      {
        "command": "flowmaster.refresh",
        "title": "FlowMaster: Refresh Dashboard"
      },
      {
        "command": "flowmaster.newDemand",
        "title": "FlowMaster: New Demand"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "flowmaster-sidebar-container",
          "title": "FlowMaster",
          "icon": "$(git-branch)"
        }
      ]
    },
    "views": {
      "flowmaster-sidebar-container": [
        {
          "type": "webview",
          "id": "flowmaster-sidebar",
          "name": "Dashboard"
        }
      ]
    },
    "configuration": {
      "title": "FlowMaster",
      "properties": {
        "flowmaster.statePath": {
          "type": "string",
          "default": ".workflow/state",
          "description": "Relative path from workspace root to the OpenFlow state directory"
        },
        "flowmaster.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Automatically refresh state when dashboard opens"
        },
        "flowmaster.terminalReuse": {
          "type": "boolean",
          "default": false,
          "description": "[DEPRECATED] No longer applicable in spawn mode — terminal sessions are managed per-demand via ProcessManager"
        },
        "flowmaster.skipPermissions": {
          "type": "boolean",
          "default": false,
          "description": "Run Claude Code with --dangerously-skip-permissions to avoid interactive permission prompts on long-running tasks"
        },
        "flowmaster.terminal.fontSize": {
          "type": "number",
          "default": 14,
          "minimum": 8,
          "maximum": 72,
          "description": "Font size for the inline terminal"
        },
        "flowmaster.terminal.fontFamily": {
          "type": "string",
          "default": "Consolas, monospace",
          "description": "Font family for the inline terminal"
        },
        "flowmaster.terminal.scrollback": {
          "type": "number",
          "default": 1000,
          "minimum": 0,
          "maximum": 50000,
          "description": "Scrollback buffer size for the inline terminal"
        },
        "flowmaster.terminal.splitRatio": {
          "type": "number",
          "default": 0.6,
          "minimum": 0.1,
          "maximum": 0.9,
          "description": "Initial split ratio for the dashboard/terminal panels (0.1-0.9)"
        }
      }
    }
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.js",
    "precompile": "npm run bump-version",
    "compile": "tsc -p tsconfig.json",
    "watch": "tsc -watch -p tsconfig.json",
    "lint": "tsc --noEmit --strict",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-icon": "node scripts/generate-icon.js",
    "prepackage": "npm run compile",
    "package": "npx vsce package --no-dependencies",
    "publish": "npx vsce publish --no-dependencies"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0",
    "xterm-addon-web-links": "^0.9.0",
    "yaml": "^2.7.0"
  }
}
